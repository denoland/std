Deno.test('daemon is listed', async () => {
  // test daemon is listed immediately
  // listed when reply is present
  // listed when request has been blanked
})

// test that branches have the same record keeping
// test that branches are cleared when the branch ends
