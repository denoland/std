steps:
  - bash: curl -L https://deno.land/x/install/install.sh | sh -s $(DENO_VERSION)
  - bash: echo '##vso[task.prependpath]$(HOME)/.deno/bin/'
  - bash: echo '##vso[task.setvariable variable=DENO_DIR;]$(HOME)/deno_dir/'
  - bash: mkdir -p $(DENO_DIR)
