/**
 * Test giving a prompt, calling the stuck-finder, then calling the help it
 * returns.
 *
 * Then expand it to create its own stucks where it found no help.
 * Get to store all invocations of the helps and rate its responses.
 */

Deno.test.ignore('create a new stuck directly', () => {})
Deno.test.ignore('create a new stuck in the background', () => {})
Deno.test.ignore('e2e create a new stuck then use it', () => {})
