import React from 'react'

const Artifact = ({ wipe }) => {
  // make a provider that can pass down the artifact object to any children
  // boot Artifact, and handle any initialization jobs
  return <div>ASDF</div>
}

export default Artifact
